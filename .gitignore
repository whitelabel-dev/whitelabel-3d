.vercel

# samples/ is COMMITTED so the deployed viewer at 3d.whitelabel.dev
# has demo files available. Keep them small (<50MB each) so deploys
# stay quick.
# (intentionally not ignoring viewer/samples/*)
